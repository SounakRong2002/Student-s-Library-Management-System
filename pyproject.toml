[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "student-library-management"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
  "streamlit",
  "pandas",
]

[project.scripts]
app = "app:main"

